version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: precar_inspect_app
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - NEXT_PUBLIC_SUPABASE_URL=http://127.0.0.1:54321
      - NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJvbGUiOiJhbm9uIn0.mock-signature
      - NEXT_PUBLIC_APP_URL=http://localhost:3000
    restart: always
